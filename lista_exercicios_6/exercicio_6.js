let calcularAreaRetangulo = (largura, altura) => {
    let areaRetangulo = largura * altura;
    console.log(`A área do retângulo é: ${areaRetangulo}`);
}

calcularAreaRetangulo(3,6);
calcularAreaRetangulo(100,7);